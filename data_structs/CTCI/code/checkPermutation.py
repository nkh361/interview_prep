def checkPermutation(word_one, word_two):
    """
    word_one_list = []
    word_two_list = []
    for i in word_one:
        word_one_list.append(i)
    for k in word_two:
        word_two_list.append(k)
    word_one_list.sort()
    word_two_list.sort()
    if len(word_one_list) != len(word_two_list):
        return False
    else:
        for i in range(len(word_one_list)):
            if word_one_list[i] = 
    """
